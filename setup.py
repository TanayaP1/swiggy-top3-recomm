from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project recommends the top 3 restaurants for a food app.',
    author='Tanaya',
    license='',
)
